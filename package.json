{
  "name": "zero-hour-rts",
  "version": "0.5.0",
  "description": "Original real-time strategy engine inspired by Command & Conquer: Zero Hour. Runs on Windows and macOS via the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
